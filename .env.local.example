# Production URL for metadata/OG (optional; Vercel sets VERCEL_URL automatically)
# NEXT_PUBLIC_APP_URL=https://yourdomain.com

# Supabase — Dashboard → Settings → API (or Connect dialog)
# Project URL + publishable key (sb_publishable_... format)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Or use this if Supabase gives you this name:
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# Secret key (API Keys tab) or service_role (Legacy) — server-only, bypasses RLS
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=

# — Optional —
# Premium model for complex summaries (legal, meeting, chapters, core)
# Leave unset → uses gpt-4o. Set to gpt-4o-mini to save cost.
DOCAI_PREMIUM_MODEL=

# Embedding batch size (default 20)
# DOCAI_EMBED_BATCH_SIZE=20
